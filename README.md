# PurchasedFromAds
This project applies a Support Vector Machine (SVM) to classify whether a user will purchase a product based on Age and Estimated Salary. The model is trained on scaled data, evaluated using accuracy and a confusion matrix, and visualized with decision boundaries.
